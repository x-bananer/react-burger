import styles from '../order-details/order-details.module.css';

const OrderDetails = ({  }) => {
    return (
        <div className={styles['order-details']}>ORDER</div>
    );
};

export default OrderDetails;